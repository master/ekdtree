{define, version, {shell, "echo -n `git describe --tags --abbrev=0`"}}.

{profiles, [
    {default, [
        {prepare, [clean]}
    ]}
]}.


{cc, [
    {ekdtree, [
        {linux, [
            {ldflags, "-lCGAL -lstdc++ -lm"},
            {cflags, "-frounding-math"}
        ]}
    ]}
]}.
